---
scope: Culvert job_control/audit surface: event-sourced audit+FinOps model to match docs/CONTRACT.md §4/§5/§6 (normative, direction A), plus append-only JobControlRepository (review findings #4/#12)
purpose: One spine for two rebuilds that share the job_control dataset, the event model and the contract_version stamp, so they cannot diverge
altitude: epic
updated: 2026-09-08T08:26
---

- (direction) Joseph chose direction (A) 2026-09-04: docs/CONTRACT.md is NORMATIVE; the shipped code is rebuilt event-sourced to match it. Rejected (B) rewrite-doc-from-code and (C) mark-contract-aspirational.
- (constraint) RELEASED SURFACE: Culvert 0.1.x is live on PyPI (culvert) and Maven Central (com.enrichmeai.culvert:*). AuditRecord is mirrored field-for-field across languages (AuditRecord.java:32-43 = 11 fields, records.py:24-34 = 11 fields). Any change to it breaks two published ecosystems, so it needs a deprecation path, not an edit.
- (constraint) TWO LIVE EMITTERS, not one: BigQueryAuditEventPublisher (Java) and PubSubAuditPublisher (Python, deployments/postgres-cdc-streaming/.../audit.py:30-39) which serialises via dataclasses.asdict and therefore breaks on ANY field change to the dataclass.
- (constraint) FOUR INCOMPATIBLE SHAPES for one table name today. (1) docs/CONTRACT.md:97-108 job_control.audit_events, event-sourced, 10 cols. (2) BigQueryAuditEventPublisher.java:191-200 INSERT of 12 cols into default audit.audit_events (:93,:96), stage-summary model. (3) provisioned DDL job_control.audit_trail = shape 2 minus metadata_json/published_at (03_create_infrastructure.sh:180-182, main.tf:646-668). (4) Grafana consumer expects event_type/error_code/records_rejected (grafana-dashboards-configmap.yaml:595). Shapes 1 and 2 share exactly ONE column: run_id.
- (constraint) CONTRACT GOVERNANCE: docs/CONTRACT.md:20 - major bump needs a full cross-team migration plan, old producers must stop before new consumers go live. :289-296 - the change PR must bump CONTRACT_VERSION, update tests/contract/fixtures/ and tests/contract/test_conformance.py, and be approved by a maintainer per producing/consuming team. Those fixture and test paths DO NOT EXIST, so the doc's own change process cannot currently be executed as written.
- (constraint) SILENT FAILURE is the repo-wide pattern this epic exists to kill: BigQueryAuditEventPublisher catches every write exception and logs WARN 'audit error swallowed' (:238-243). Combined with the wrong dataset/table, the audit trail has never worked in production and never said so. Story 1.6.
- (constraint) CONTRACT SECTIONS WITH NO EMITTER AT ALL: §6 job_control.reconciliation_record (docs/CONTRACT.md:175-189) has no producer in either language - only a Grafana consumer references it. §10.6 promises a shipped conformance suite at tests/contract/ that does not exist.
- (decision) ONE LOG: job_control.audit_events IS the job-control log. JobControlRepository writes become appends; pipeline_jobs becomes a latest-state VIEW, not a table. Binds: every job-control write path, both emitters, the DDL, the Grafana consumers. Prevents: a shadow job-control model outside the contract - the exact mechanism that produced four incompatible shapes. Rationale: direction (A) makes the contract normative, and pipeline_jobs appears nowhere in it; §4's event_kind enum already IS the job-control lifecycle. Accepted cost: latest-state reads become a window function over an event log rather than a point lookup - affordable at one run per entity per day, would not be at 1000x.
- (decision) Story 1.3's compare-and-set SEMANTICS survive the move to append-only: a transition that changes nothing must not report success. Re-expressed as an append-time guard rather than a DML precondition. Prevents: silently discarding the defect 1.3 fixed while replacing its mechanism.
- (constraint) NO EXTERNAL CONSUMERS (Joseph, 2026-09-04): 'we have no real users - we can do what is best'. The 0.1.x PyPI/Maven publication is real but nobody depends on it. This RETIRES the deprecation-path constraint entirely: no dual-write, no compatibility shim, no parallel types. Get the shape right rather than migrate to it. Revisit condition: the first external adopter.
- (decision) CLEAN BREAK at 0.2.0: AuditRecord (stage-summary, 11 fields) is REPLACED by an event-sourced AuditEvent matching docs/CONTRACT.md §4. No deprecation window. Binds: AuditRecord.java, records.py, both emitters, every test double. Prevents: two audit shapes live at once - which a dual-write window would have reintroduced, recreating the very condition this epic exists to remove. 0.1.x stays published and frozen.
- (decision) TABLE NAMING follows the contract, not the DDL: the target is job_control.audit_events (docs/CONTRACT.md:97). The provisioned job_control.audit_trail is renamed, and BigQueryAuditEventPublisher's default target of <project>.audit.audit_events (:93,:96) is corrected. Binds: 03_create_infrastructure.sh:180-182, main.tf:646-668, the Grafana dashboards, the e2e scripts. Prevents: the audit_events/audit_trail collision persisting as a fifth shape. Resolves Story 1.6's wrong-dataset/wrong-table half.
- (decision) EVENT GRAIN IS BOUNDED PER RUN: RECORD_VALIDATED and RECORD_REJECTED are emitted ONCE per run with counts in payload; per-row detail stays in the existing quarantine blobs, with the quarantine URI carried in the RECORD_REJECTED payload. Binds: every emitter. Prevents: a literal reading of §4 emitting one BigQuery row per input row - ~5,000,003 rows for a 5M-row extract, duplicating what quarantine already stores. Audit volume is now O(1) per run, not O(rows).
- (decision) FAILURE SEMANTICS ARE SPLIT BY EVENT CLASS: a failed append of a run-level event (RUN_START, RUN_END, ERROR_RAISED, RECONCILIATION) THROWS and fails the pipeline - these ARE the run's state. A failed aggregate RECORD_* append logs ERROR, writes a dead-letter blob, and continues. Binds: both emitters, every catch block. Prevents: the swallow at BigQueryAuditEventPublisher.java:238-243 surviving the rebuild; and equally prevents coupling ingestion availability to BigQuery for a counter nobody reads in an incident. NO append may be silently swallowed under any circumstances.
- (decision) FAILURE IS STICKY IN THE LATEST-STATE VIEW: run state is NOT simply the most recent event. If an ERROR_RAISED or a failed RECONCILIATION exists for a run_id, the run is FAILED regardless of any later RUN_END. Binds: the pipeline_jobs view, getJob, getPendingJobs, getEntityStatus, and every consumer of run status. Prevents: BOTH (a) the ambiguity of two events sharing an event_ts, since terminal-state precedence decides rather than recency, AND (b) external review finding #1 recurring at the storage layer - a SUCCEEDED appended after a failure can no longer make a bad run read green. The advisor correctly noted append-only ALONE does not fix #1; this precedence rule is what closes it.
- (decision) CONTRACT_VERSION comes from ONE constant per language in the core package (data-pipeline-core / data_pipeline_core), never duplicated per adapter, and is stamped by the event constructor rather than by each emitter. Binds: every emitter in both languages. Prevents: per-adapter drift in the value, and an adapter forgetting to stamp it at all. Satisfies Story 1.4 AC1.
- (decision) APPENDS ARE IDEMPOTENT BY (run_id, event_kind) for run-level events: re-appending RUN_START for an existing run_id is a no-op, not a duplicate row and not an error. Binds: createJob and every retry path. Prevents: a retried Airflow task doubling a run's lifecycle events, which would corrupt the latest-state view. Carries forward Story 1.3's semantics - a transition that changes nothing must not report success - as an append-time guard.
- (decision) §10.6's CONFORMANCE SUITE IS BUILT, NOT DROPPED: tests/contract/ with fixtures, exercised from both languages. Binds: the contract change process itself. Prevents: docs/CONTRACT.md:289-296 remaining unexecutable - it requires updating fixtures that do not exist, so today the contract's own governance cannot be followed. Also gives the four-shapes problem a permanent guard rather than a one-off cleanup.
- (decision) ONE OWNER FOR THE job_control DDL: Terraform (infrastructure/terraform/systems/generic/main.tf) is authoritative; scripts/gcp/03_create_infrastructure.sh stops declaring these table schemas and either calls Terraform or is retired for them. Binds: both provisioning paths. Prevents: the current state where ONE table's schema is declared in TWO places (main.tf:646-668 and 03_create_infrastructure.sh:180-182) that already disagree with the emitter and with each other - a fifth shape waiting to happen. Two owners of one schema is the exact divergence an invariant exists to stop.
- (decision) NO DATA MIGRATION: the existing audit_trail/audit_events tables are dropped and recreated, not migrated. Rationale: the emitter has never successfully written (wrong dataset, wrong table, two absent columns, failures swallowed), so there is no production audit data to preserve; and the int environment is explicitly ephemeral by project policy. Prevents: a migration script written to move rows that do not exist.
- (decision) THE CONFORMANCE SUITE IS THE JAVA/PYTHON PARITY GUARD: §10.6's tests/contract/ fixtures are shared by both languages, so agreement is enforced by both reading the same fixtures rather than by a separate mirror test. A MISSING counterpart must FAIL, never skip. Binds: both language test suites. Prevents: external review finding #13 - every contract claims 'Java mirror of the Python Protocol' with nothing enforcing it - and specifically prevents the sibling-project failure mode the review cited, a parity test pointed at a deleted directory that skipped silently while CI stayed green.
- (decision) THE JobControlRepository CONTRACT TEST ASSERTS APPEND-ONLY BEHAVIOURALLY, not by inspecting SQL: after a failure event followed by a success event, the repository must report the run as FAILED. Any backend passing this is implementable on a store without UPDATE. Binds: every JobControlRepository implementation. Prevents: a backend satisfying the letter of append-only while its reads still return last-write-wins - and gives the Athena claim in review finding #4 a falsifiable test rather than an assertion.
- (decision) ATHENA IS BUILT, NOT JUST UNBLOCKED (Joseph, 2026-09-04): this epic ships AthenaJobControlRepository passing the same contract test as BigQuery and DynamoDB. Turns review finding #4's central claim - that mutation blocks Athena - from an argument into a shipped artifact. DynamoDB is rebuilt too; no adapter is left mutating against an append-only contract.
- (decision) GRAFANA DASHBOARDS ARE UPDATED IN THIS EPIC: grafana-dashboards-configmap.yaml is ported to the event model (event_type -> event_kind, error_code and records_rejected -> payload fields, audit_trail -> audit_events). Binds: the dashboard configmap. Prevents: shipping a knowingly-broken consumer, which is precisely how four shapes accumulated.
- (question) OPEN: §6 job_control.reconciliation_record has no producer in either language. The RECONCILIATION event kind in §4 now carries reconciliation outcomes, so §6 may be redundant with §4 rather than a separate table. Not resolved here - flagged for the build to settle with evidence, since collapsing a contract table is a bigger call than this spine should make unilaterally.
- (event) Reviewer gate: lint_spine clean (0 findings). Rubric-walker returned REVISE BEFORE STORIES with 9 findings; the two load-bearing ones independently verified by the architect before acceptance.
- (constraint) VERIFIED (architect, not taken on faith): TWO shadow job-control writers already exist OUTSIDE the port. deployments/fdp-trigger/src/fdp_trigger/job_control.py:29 INSERTs a RUNNING row straight into job_control.pipeline_jobs; deployments/postgres-cdc-streaming/src/streaming_pipeline/pipeline/job_control.py is a second unregistered implementation. AD-1 claims to prevent a shadow model that is already present in two places - the spine must bind them explicitly or it is describing a world that does not exist.
- (constraint) VERIFIED: AD-6 as drafted is WRONG against the contract. docs/CONTRACT.md:252 (§9) mandates emitting RETRY_ATTEMPTED before EACH retry, capped at 3 attempts, so up to 3 such events legitimately share one run_id - idempotency keyed on (run_id, event_kind) would collapse them to one. Separately :226 (§7) requires a RETRIED PIPELINE to take a NEW run_id, recording the old one in payload.previous_run_id, which the spine did not account for at all.
- (version) VERIFIED ON THE WEB by the currency lens: Athena INSERT INTO IS supported on non-Iceberg Hive tables (Avro/Ion/JSON/ORC/Parquet/Text/CSV; only bucketed tables and federated queries excluded). Iceberg is NOT required. AD-13's commitment to build AthenaJobControlRepository is therefore feasible as committed - the spine's critical premise survives.
- (constraint) VERIFIED by architect: AD-9's evidence was wrong even though its rule was right. The two audit_trail declarations are column-for-column IDENTICAL. The genuinely divergent pair is pipeline_jobs - main.tf:597-644 (23 cols) vs 03_create_infrastructure.sh:147-175 (16 cols) - which the spine did not cite. Fix the citation, keep the rule. Third declarer also exists at setup_cdp_segment_infra.sh:108.
- (constraint) VERIFIED by architect: Stack row 'AWS SDK - as pinned in the reactor parent' is FALSE. No such property exists in data-pipeline-libraries-java/pom.xml; aws.sdk.version 2.25.31 is duplicated across SIX module poms (s3, sqs, secrets, athena, cloudwatch, dynamodb) and is far behind upstream 2.54.x. Also corrected: the §4 heading is docs/CONTRACT.md:92, not :97.
- (constraint) VERIFIED claim from currency lens: payload JSON has NO Athena type - Hive has none and AthenaWarehouse.java:401-418 falls through to varchar. So AD-11's cross-language parity cannot promise column-level byte-equality for payload; it must promise semantic equality after JSON parse. AD-6's (run_id, event_kind) idempotency is additionally NOT atomically implementable on non-Iceberg Athena: no conditional insert, no unique constraint, no transaction.
- (constraint) CRITICAL, VERIFIED BY ARCHITECT - AD-2 + AD-3 TOGETHER CREATE A DATA-LOSS PATH. Chain: BigQueryJobControlRepository:613-617 currently requires prior state RUNNING for SUCCEEDED - that CAS guard is what blocks a late event flipping a finished run. AD-2 removes CAS. AD-3 then lets a late first ERROR_RAISED make the projection read FAILED. FAILED is retryable (:618-621), so RetryOrchestrator:111 calls cleanupPartialLoad, which is DELETE FROM WHERE _run_id = @run_id (:541) - deleting the data the SUCCESSFUL run loaded. Neither AD is wrong alone; together they destroy data. The spine MUST add a terminal-immutability rule: once a run reaches a terminal state, later contradicting events are recorded but do not change the projection, and never make it retryable.
- (constraint) VERIFIED BY ARCHITECT - LIVE PRE-EXISTING BUG, independent of this spine, deserves its own issue: JobStatus emits LOWERCASE wire values (JobStatus.java:12-14: created/running/succeeded/failed) but deployments/fdp-trigger/src/fdp_trigger/dedup.py:38 filters status IN ('RUNNING','SUCCESS') - wrong case AND 'SUCCESS' is not a member of the enum at all (it is 'succeeded'). The dedup query therefore matches zero rows, raises nothing, and duplicate Dataflow launches are happening in production today. The spine must fix ONE status vocabulary; §4 has no status column, so the projection must define it.
- (constraint) VERIFIED claim: AD-15's 'never written, safe to drop' proof holds for audit_trail ONLY. It does NOT hold for pipeline_jobs, which has a live in-repo writer (fdp-trigger job_control.py:54, streaming inserts), an external writer, and four live readers - including _job_control.py:60-61 whose QUALIFY ROW_NUMBER() ORDER BY updated_at DESC is exactly the recency semantics AD-3 forbids. A streaming insert against a VIEW hard-fails, and it fires AFTER launch_segment_transform has already committed.
- (constraint) The projection is under-specified relative to the log - this is where every integrity risk sits. §10.3 (CONTRACT.md:265) forbids extra columns in well-known tables, forcing ~18 live-read fields into payload JSON, and no AD binds the payload KEY names. A path mismatch then returns NULL silently where today's typed schema fails loudly. The revision must bind the payload key contract and the view's column list explicitly.
- (constraint) Typo correction to the DATA-LOSS PATH entry above: shell backtick-stripping ate a word. The statement at BigQueryJobControlRepository.java:541 reads DELETE FROM (backtick-quoted tableId) WHERE _run_id = @run_id - i.e. it deletes from the caller-supplied TARGET TABLE, not from job_control.
- (constraint) CRITICAL, VERIFIED BY ARCHITECT: lifecycle ignore_changes = [schema] is set on BOTH job_control tables in infrastructure/terraform/systems/generic/main.tf - on pipeline_jobs and on audit_trail. AD-9 (Terraform authoritative for schema) and AD-15 (drop and recreate) would therefore PLAN GREEN AND CHANGE NOTHING. A silent no-op on the epic's critical path, in the very tool the epic uses to fix silent failures. The revision must require removing that lifecycle block and must verify the schema actually changed, not that terraform apply exited 0.
- (constraint) VERIFIED BY ARCHITECT: PipelineJob is a 23-component record (PipelineJob.java) against §4's 10 columns. Only run_id/system_id/entity/event_kind/event_ts/extract_date map directly; roughly 19 fields must live in payload JSON and NO AD binds their key names. Two adapters would diverge on markFailed alone. The revision MUST bind a payload key contract per event_kind.
- (constraint) VERIFIED grain error: AD-4 keys events per RUN, but the read model needs per (run_id, entity) - getEntityStatus(systemId, extractDate) returns a List<EntityStatus> (JobControlRepository.java:58) and is unimplementable from per-run aggregates, or silently lossy. Event identity is (run_id, entity, event_kind), not (run_id, event_kind).
- (event) Reviewer gate complete: 4 lenses, all 4 returned changes-requested or revise. Verdicts: rubric REVISE BEFORE STORIES, currency SOUND-with-3-wrong-claims (Athena INSERT INTO premise CONFIRMED), adversarial PASS WITH CHANGES (9 obey-and-diverge pairs), data-integrity CHANGES REQUESTED (11 blocking). Architect independently verified every load-bearing claim before acceptance; 3 of the architect's own ADs were factually wrong (AD-6 vs §9 retry, AD-9 evidence, Stack AWS SDK row).
- (event) Deliverables complete: ARCHITECTURE-SPINE.md (revised post-gate, lint clean), MIGRATION-PLAN.md (6 phases, recommends splitting the epic), walkthrough.html (published artifact). Spine status remains draft pending Joseph's scope call on the epic split.
- (event) spine finalized
- (constraint) PREMISE REVERSED 2026-09-08 (Joseph): 'all libraries are contributing to the valuedocs company'. This RETRACTS the 2026-09-04 constraint that no external consumers exist, which was the stated justification for AD-8's clean break and for shipping Warehouse.loadFromUri as a breaking change with no overload. Consequences: (1) AD-8 must be RE-TAKEN when Phase 1 actually builds AuditEvent, not assumed - a deprecation window is back on the table; (2) every shipped breaking change now needs a MIGRATION.md entry - written for 0.2.0's loadFromUri; (3) docs/CONTRACT.md section 11's requirement for approval from each producing/consuming team now has a real second team behind it; (4) region choice for any valuedocs-facing deployment is a data-residency question (legal documents), not a cost one.
