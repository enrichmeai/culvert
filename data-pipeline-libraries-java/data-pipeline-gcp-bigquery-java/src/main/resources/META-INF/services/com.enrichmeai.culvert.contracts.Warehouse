# Auto-config registration. BigQueryWarehouse has a no-arg constructor that
# self-configures from GCP_PROJECT/GCP_LOCATION (BigQueryDefaults) for
# worker-side ServiceLoader reconstruction; the (projectId, client) constructor
# is used driver-side.
com.enrichmeai.culvert.gcp.bigquery.BigQueryWarehouse
